# virtual-music-gerua-
Clone &amp; execute the repo and enjoy the song lines of "Gerua" by Arijit singh virtually. 
Required Modules -- pygame,sys,time,random. 
The sys, time, and random modules are part of Python's standard library, so they're included by default. However, pygame needs to be installed.
For installing pygame just run -> pip install pygame on terminal. 
